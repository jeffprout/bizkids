# Cloudflare Pages reads this file from the build output.
# Hashed asset filenames never change contents, so they can be cached forever.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# Everything else must revalidate, so a reload picks up a new deploy rather
# than a stale index during a playtest.
/*
  Cache-Control: no-cache
